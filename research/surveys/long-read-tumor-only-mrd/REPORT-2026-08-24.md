# Daily report — 2026-08-24

**Subject:** why a day of literature survey ended up testing a within-sample statistic, and why
that statistic was falsified the same day.

**A note on naming.** This report deliberately gives the tested statistic **no proper name**. It is
not a new method belonging to this project — it is one instance of a published family (see §2).
It is referred to by description: **allele fraction computed within a single haplotype**. Giving it
a name of our own would be claiming authorship of something that is not ours.

**One-line conclusion.** The survey's primary direction was falsified by the failure criterion it
registered before the run — but the gap it was invented to close is still there, still measured,
and does not depend on that direction having been right.

---

## 1. The question the day opened with: what is binding?

The day did not begin with "which idea should we try". It began with **which constraint blocks
everything else** — the binding constraint.

The answer came from a finding that already existed:

> `FIND-0002` — **30.4%** of clustered PASS candidates carry their ALT allele on **≥2 reads of the
> matched normal**, meaning inherited variation rather than tumor variation. **65.9%** of those are
> absent from the SEQC2 somatic truth set.

Why this is binding rather than merely a bad number: **every other research direction in this
project sits downstream of it.** Methylation, same-molecule linkage, MRD detection — all operate on
the candidate set. If ~30% of that set is inherited variation, every statistic downstream is
measuring partly noise. There is no way around it.

**Consequence for direction-picking:** candidate-set composition was ranked as the problem to solve
first.

---

## 2. Literature review — what the field says, and where the gap is

### 2.1 The field says this is already solved

The systematic search of 2026-08-21 and the 80-source deep research run today converge on the same
conclusion:

| Source | Contribution |
|---|---|
| **SGZ** (Sun et al., PLoS Comput Biol 2018) | Somatic/germline separation without a matched normal, using allele fraction plus purity/ploidy/copy number |
| **smrest** (Simpson 2024, PMC10925087) | **Haplotype phasing** as the primary information source for tumor-only somatic calling on long reads |
| **ClairS-TO** (Nat Commun 2025) | Ships `MultiHap`, `NoAncestry`, and `Verdict` — Verdict described in the paper as *"similar to and improved from SGZ"* |
| **UNMASC** (PMC8494212) | An unmatched-normal design |

The lab's conclusion on 2026-08-21, verbatim: *"not a research question — it is an integration
task"*.

**That conclusion is correct about the literature.** Stopping there closes the direction.

### 2.2 But counting it on our own data does not agree

`[measured 2026-08-24]` — `week4/expA_full/HCC1395_pure/snv.vcf.gz`, 3,169,996 records / 48,819 PASS:

| Tag | On PASS candidates | Anywhere in the file |
|---|---|---|
| `H` (haplotype-support flag) | 28,799 / 48,819 (**59.0%**) | — |
| `MultiHap` | **0** | 204 records (0.0064%), all co-tagged `LowQual` |
| `NoAncestry` | **0** | 242 records (0.0076%), all co-tagged `LowQual` |
| `Verdict` | **0** | 0 — disabled at launch (`--disable_verdict`, missing `alleleCounter` and the CNA bundle) |

To check this is not a configuration artifact of one run, the same census on **four independent
tumor-only ONT runs** on this server, each of which completed its phasing step:

| Sample | Records | PASS | `MultiHap` | `NoAncestry` |
|---|---|---|---|---|
| H1437 | 3,415,364 | 65,448 | 153 | 134 |
| H2009 | 3,447,798 | 153,854 | 173 | 391 |
| HCC1937 | 3,124,945 | 29,503 | 147 | 73 |
| HCC1954 | 3,775,749 | 74,996 | 145 | 264 |
| **HCC1395 (this project)** | 3,169,996 | 48,819 | 204 | 242 |

Across five independent samples the two haplotype-based germline separators touch **0.002–0.011%**
of records and reach **no PASS candidate at all** in this project's run.

`NonSomatic` alone accounts for 3,082,216 records — the population panel-of-normals is doing
essentially all of the filtering.

### 2.3 The gap, stated precisely

Both of these are true and **they do not conflict**:

- The field **has** a mechanism for matched-normal-free germline separation.
- The shipped implementation, run on this ONT tumor-only data, removes **approximately nothing**.

| One end | The other end |
|---|---|
| Real germline contamination, measured with a matched normal: **30.4%** (clustered candidates) · **28.0%** (per candidate, chr1) | Removed by the shipped filters: **0 PASS candidates** across 5 runs |

**The distance between those two numbers is the gap.** It is not the novelty of an idea — it is the
novelty of a measurement. It is the only thing produced today that could open a paper.

---

## 3. Evidence measured during the day

All measured, none inferred from documentation.

| # | Content | Numbers | Source |
|---|---|---|---|
| **E1** | The data **has been phased since week 4** and nobody noticed | 261 GB of haplotagged BAMs and phased VCFs in a `tmp/` directory. chr1: 147,165 phased heterozygous records, 161 blocks, 234.1 Mb spanned (~94% of the chromosome), **N50 = 5.26 Mb** | `01` §4.1 |
| **E2** | Allele, haplotype and methylation are co-observed on the same molecules | In the sampled window: 124/224 reads (55.4%) carry `HP` and `PS`; **224/224 (100%)** carry `MM`/`ML` | `01` §4.1 |
| **E3** | The shipped germline filters do not fire | Tables in §2.2 — 0 PASS candidates, 0.002–0.011% of records, 5 independent samples | `01` §4.2 |
| **E4** | Independent negative controls exist and had never been inventoried | 5 independent genomes (H1437, H2009, HCC1937, HCC1954, COLO829) with ONT tumor+normal, ClairS-TO output, and orthogonal truth | `01` §4.3 |
| **E5** | PDF-level verification: MRDetect anchors its threshold **parametrically** | `Z = (det_rate − μ)/σ`, μ and σ fitted over 30 control plasmas, thresholds at Z>3 and Z>4, 95–98% clinical specificity | `02` §2 (PDF verification); threshold detail: daily log 2026-08-24 |
| **E6** | This project's haplotags are the **clean** kind — no label leakage | Run log: plain `longphase phase` — no `--somaticMode`, no `--tumor-*`. Tag census: `HP:i:2` 506 reads, `HP:i:1` 12 reads, **zero `HP:Z:`**. Value domain is 1/2 only | daily log 2026-08-24 |
| **E7** | **EV-0019** — the falsification experiment's result | See §5 | `exp-s1-001/results/` |

**E6 deserves separate attention.** A colleague working on the same server recorded on 2026-08-21
that the lab's ready-made haplotag file **could not be used as a feature**, because it was produced
with `longphase haplotag --somaticMode --tumor-bam-file <pure tumor BAM>`, which gives tumor reads
`HP:Z:1-1 / 2-1 / 3` values that normal reads can never receive — so the tag itself reveals tumor
origin. Had this project's files been the same, the experiment would have been broken at its input.
Two commands answered it: they are not. This is the only finding of the day that was **verified
rather than inferred**, and the only one that came through undamaged.

---

## 4. The reasoning chain that led to the tested statistic

Five links, each anchored to a number above.

**Link 1 — Identify the binding constraint.** Candidate-set composition, ~30% private germline
contamination (§1). Every other direction sits behind it.

**Link 2 — The literature says it is solved.** SGZ / smrest / `MultiHap` / `NoAncestry` / `Verdict`
(§2.1). Normally the direction closes here.

**Link 3 — The measurement contradicts the literature.** 0 PASS candidates touched across 5
independent samples (§2.2). The gap is quantified at both ends (§2.3). **This is the decisive
link** — it turns an "integration task" back into a measurable question.

**Link 4 — The material turns out to be free.** A haplotype-based mechanism needs reads assigned to
haplotypes, which costs compute. E1 shows 261 GB already on disk since week 4. The cost of testing
drops to near zero, and this is why the direction outranked the alternatives — **equally worth
doing, far cheaper, and killable in a single run**.

**Link 5 — The mechanism, in one sentence.** An inherited variant **defines** one of the two
ancestral haplotypes, so nearly every read of that haplotype carries it → the within-haplotype
fraction ≈ 1. A **subclonal** somatic variant merely *rides* a haplotype in a fraction of cells →
that fraction < 1. Whole-locus VAF cannot see the difference because it averages across both
haplotypes.

> **This is where the argument broke, and it broke as it was being written, not when it was run.**
> Link 5 depends on the word **"subclonal"**. The reason given for believing it: `FIND-0002`
> measured HCC1395's somatic pairs as largely subclonal — median ALT-ALT fraction 0.200, 45.7%
> below 0.15.
> Read again: **0.200 is a pair-level molecule statistic, not a per-candidate one.** Measured per
> candidate today, somatic VAF is centred between **0.3 and 0.7**. Not the distribution the
> argument required.

---

## 5. The falsification experiment and its result

`EXP-S1-001` — designed to be **killable in one run**, on files that already exist, in hours rather
than days. Every threshold was fixed **before** the run (`08` §10–§11).

**Leakage guard, and how it was enforced:**

| Rule | What happened |
|---|---|
| The feature function must not see truth | Its arguments are only the tumor BAM, the candidate VCF and the reference |
| Features must be written to disk **before** any label is read | Feature step began 09:20:45Z and closed its file at 09:24; labelling began 09:26:28Z |
| The matched normal may be opened only at labelling | It was |
| No threshold tuning after seeing labels | AUC is threshold-free; the single reported operating point was fixed in advance from the mechanism |

**Result:**

| | |
|---|---|
| Candidates with features | 3,925 (chr1) |
| Meeting the inclusion rule (inside a SEQC2 high-confidence region, labelled, ≥10 reads on **both** haplotypes) | **1,627** — 556 germline, 1,071 somatic |
| AUC(whole-locus VAF) — baseline | **0.6318** [0.6023–0.6595] |
| AUC(within-haplotype fraction) — condition | **0.5972** [0.5716–0.6214] |
| **ΔAUC** | **−0.0346**, paired bootstrap 95% CI [−0.0676, +0.0015] |
| Registered H0 (ΔAUC ≤ 0.02) | **not rejected** |

**Pre-fixed operating point** (≥ 0.90 → call germline): removes 520/556 germline (93.5%) but
discards 772/1,071 somatic (72.1%); precision for germline is 0.4025. Not usable.

**Distribution — the mechanism made visible:** 468/556 germline (**84.2%**) sit at ≥ 0.95, **exactly
as predicted**. But 688/1,071 somatic (**64.2%**) sit there too. Under whole-locus VAF the two
classes peak in different places: germline at 0.60–0.75, somatic at 0.30–0.45.

**Control 1 — haplotype-swap null.** Reallocating each candidate's tagged ALT reads across its two
haplotypes at random (hypergeometric), depths held fixed: ΔAUC mean −0.0106, 95% range
[−0.0177, −0.0033]. The observed −0.0346 falls **below** that range. Conditioning on the **true**
haplotype assignment is worse than allocating the same ALT counts at random. The statistic is not
merely uninformative on this material — it is **anti-informative**.

**Control 2 — coverage stratification.** ΔAUC = −0.0078 below the median tagged depth of 75, and
−0.0604 above it. The deficit **grows with depth** rather than shrinking — the signature of a
ceiling effect, not of noise.

**Two reasons it died:**

1. **Purity.** HCC1395 is a ~100% pure tumor line. A clonal somatic variant occupies one haplotype
   **exactly as** an inherited heterozygote does. The statistic separates "occupies the whole
   haplotype" from "occupies part of it" — it does not separate "inherited" from "acquired early
   and clonally expanded".
2. **The statistic's own definition excludes the regions that need solving.** 1,895 of 3,522
   labelled in-region candidates **never reach** the comparison for want of reads on a second
   haplotype, and **809 (42.7%) have zero reads** on one haplotype. Those are the hemizygous and
   loss-of-heterozygosity regions — where germline contamination is worst. **The method is defined
   where the problem is mild and undefined where it is severe.** More chromosomes do not fix this.

**The failure criterion, written before the run** (`08` §11): *"If ΔAUC ≤ 0.02, this direction is
abandoned. No re-analysis with a different statistic may be substituted after the fact."* →
**The direction is abandoned.** The publication plan stops at stage S1; S2–S5 do not run.

---

## 6. Findings registered during the day

| Id | Content |
|---|---|
| **FIND-0004** | Allele fraction computed within a single haplotype does not separate germline from somatic better than whole-locus VAF on this material. Observation and interpretation kept apart; denominator, caveats and a "what this does not show" section recorded in full |
| **EV-0019** | The evidence record behind FIND-0004 — commands, versions, inputs, denominators |
| **LOG-2026-08-24** | Daily log, day closed, passes the validator |
| `07` §R1 | Marked **FALSIFIED**, with the original proposal text left unedited as the record of what was proposed |
| `PUBLICATION-PLAN` §8 | The outcome of S1 and what it does to the plan |

**What FIND-0004 deliberately does NOT claim:**

- Not that haplotype information is useless for tumor-only calling in general. Only that **this
  statistic, on a ~100%-purity sample,** cannot work — because the two classes become structurally
  identical under it.
- Not that the 28.0% germline contamination is irremovable by some other within-sample statistic.
  Only that **this one** does not remove it.
- The setting where the premise would differ is a **low-purity** sample — and that is precisely the
  setting this material cannot test, because at 0.01% tumor fraction there is no tumor read
  population at a locus to assign a haplotype to.

---

## 7. Literature-review gaps still open

This is what could not be closed, and it bears on any novelty claim made later.

| # | Question | Status |
|---|---|---|
| **D4** | Does smrest already contain this exact statistic? | **Abstract only.** Full text not retrievable. If it does, novelty drops from 3 to 1 |
| **D8** | Does *"Read-level DNA methylation deconvolution enhances circulating tumor DNA detection"* (PMC12536878) already contain the premise of the per-molecule methylation direction? | Title only. **That direction's novelty score was downgraded to "provisional"** rather than silently kept |
| **D9** | Does *"Ultra-sensitive MRD detection through WGS with single-read error correction"* (PMC11393307) pre-empt the read-level background-suppression framing? | Not retrievable |
| **D5** | Does UNMASC's unmatched-normal design pre-empt the cross-sample background direction? | Abstract only |
| **D6** | Is the PhasED-Seq fragment-length constraint sourced? | The repository holds the validation paper, not the source. Open since 2026-08-21 |

**Technical cause, recorded in `10-provenance` §6.2.** PMC, Europe PMC, bioRxiv, nature.com,
academic.oup.com and embopress.org are all WAF-blocked from this environment. More dangerous:
**captcha interstitial pages were still marked `ready`** by NotebookLM and would have been cited as
real sources. 22 junk sources (15 captcha pages of ~450 characters, 7 outright failures) were
deleted from two notebooks during the day.

Reachable: PLOS, Frontiers, PNAS, Taylor & Francis, MDPI, medRxiv (PDF), doi.org.

**The cheapest outstanding task:** download four PDFs by hand from a machine with an ordinary
browser — PMC10925087 (smrest), PMC12536878, PMC11393307, PMC10155347 — and load them into the
notebook. Until then, **no novelty claim is defensible.**

---

## 8. Errors made during the day, recorded as they happened

| Error | Impact | Handling |
|---|---|---|
| Reported that `research_import` fetches PMC without trouble — stated without checking | 22 junk sources across 2 notebooks, marked `ready` and citable; 6 had been sitting there since an earlier import under a claim that it had succeeded | All deleted, both notebooks re-listed to confirm. New rule: call `source_wait` and read the warning field before treating any source as usable |
| Quoted `MultiHap` at 195 and `NoAncestry` at 235 — added rows from a truncated top-15 table instead of counting | Two wrong numbers reached the survey documents | Counted exactly: 204 and 242. Percentage range corrected from 0.004–0.011% to 0.002–0.011% |
| Recommended a four-cell-line cohort as negative controls **without checking which model produced our own candidate set** | The model is `ont_r10_dorado_sup_5khz_ssrs`; the lab knowledge base records that ClairS SSRS training **includes** those four lines. Background would be depleted where it should fire → specificity inflated | Recorded. Three repairs already exist on this machine (COLO829, which carries no training record; the synthetic-only `ss` model call sets; and the chr1 held-out convention). Found by reading a colleague's work log, not by finding it ourselves |
| Misread `FIND-0002` — took a pair-level statistic as evidence for a per-candidate property | This is the premise of link 5, i.e. of the whole experiment | Recorded in FIND-0004 and `PUBLICATION-PLAN` §8. This is an error **upstream** of the experiment |
| The null-control verdict line in the scoring script tested only one tail | Read literally, it understates the result | Corrected to two-sided and re-run; **the pre-fix output is retained**. The correction was made **after** the result was known, so it is recorded as a caveat on FIND-0004 rather than left implicit |

**On where the errors come from.** Three of the first four are **knowledge errors**, not
experimental-discipline errors: stating without checking, adding instead of counting, recommending
controls without checking the provenance of our own data. The systematic search of 2026-08-21 was
the fix for **external** knowledge; there is still **no equivalent step for knowledge held inside
the lab** — the model-contamination problem had been documented three days earlier by someone ten
metres away.

---

## 9. What remains after the primary direction died

Three things, none of which depend on the falsified statistic:

| | |
|---|---|
| The shipped matched-normal-free germline filters remove | **0 PASS candidates**; 0.002–0.011% of records across **5** independent ONT tumor-only runs |
| Private germline surviving into the tumor-only PASS set, chr1, per candidate | **28.0%** (1,098 / 3,925) |
| Best within-sample discrimination found so far | **whole-locus VAF at AUC 0.63** — and haplotype conditioning makes it **worse** |

The third number deserves separate attention: **the baseline is stronger than assumed.** Any future
proposal has to beat 0.63, not 0.5.

### The decision belongs to a person, not to a tool

`PUBLICATION-PLAN` §8 proposes a reframing — **from** *"we propose a method"* **to** *"we benchmark
the problem and show that the shipped solutions do not work on this platform"*:

> **How much germline survives tumor-only somatic calling on ONT long reads, and which
> matched-normal-free signal removes any of it?** — measured across the six cell lines on this
> server against public truth, over ClairS-TO (`H`, `MultiHap`, `NoAncestry`, and `Verdict` once
> built) and DeepSomatic tumor-only, whose outputs already exist.

That is a **benchmark** contribution rather than a **method** contribution. `PROJECT.md` reserves a
change of research direction to the researcher. **Nothing has been started on it.**

**Honest position on venue:** benchmarks do reach Genome Biology and Briefings in Bioinformatics,
but usually with more breadth than six cell lines and one platform. Do not promise Q1.

---

## 10. Provenance

**Run during the day:** the ten-phase survey → 12 documents; environment re-verification (one
discrepancy: PyYAML is 6.0.3, the docs say 5.4.1); PDF-level verification of MRDetect; deep research
over 80 and then 72 sources; a cross-read of a colleague's work log (4 entries, 20–23 August);
`EXP-S1-001` (3 scripts, 2 result files).

**Experiment tooling:** pysam 0.24.0, numpy 2.2.6, Python 3.10.12, RNG seed 42. No GPU, no fitted
model, no new installation.

**Cost:** one afternoon, one chromosome, one CPU core.

**What that cost bought:** the end of a research direction by measurement rather than by argument.
No amount of further reading would have found this — the decisive confounder **had already been
written down**, as item (i) in the proposal's own confounder list, and was still filed as "a caveat
to report" rather than "the thing that could decide the outcome".

---

*References: `FIND-0002`, `FIND-0003`, `FIND-0004`, `EV-0014`, `EV-0018`, `EV-0019`,
`LOG-2026-08-24`, `EXP-S1-001`. Survey documents: `00`–`10`, `SURVEY_SUMMARY`,
`PUBLICATION-PLAN`.*
